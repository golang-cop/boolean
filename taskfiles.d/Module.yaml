version: "3"

tasks:
  init:
    desc: Initialize the Go module
    cmds:
    - |
    
      go mod init github.com/golang-cop/boolean

  requirements:
    desc: Add module requirements and sums
    cmds:
    - |
    
      go clean -modcache -cache
      go get github.com/golang-cop/string
      go get github.com/golang-cop/result
      go mod tidy
